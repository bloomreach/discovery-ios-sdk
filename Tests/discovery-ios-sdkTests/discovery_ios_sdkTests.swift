import XCTest
@testable import discovery_ios_sdk

final class discovery_ios_sdkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(discovery_ios_sdk().text, "Hello, World!")
    }
}
